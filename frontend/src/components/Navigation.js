import React, { Fragment } from "react";

const Navigation = () => {
  return (
    <Fragment>
      <nav>
        <img src="/img/white-logo.png" alt="logo" className="logo" />
      </nav>
    </Fragment>
  );
};

export default Navigation;
